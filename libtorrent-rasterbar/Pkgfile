# Description:  A C++ bittorrent library.
# URL:          https://www.rasterbar.com/products/libtorrent/
# Maintainer:   Stamatin Cristina, dear dot volgk at gmail dot com
# Depends on:   boost

name=libtorrent-rasterbar
version=1.2.10
release=1
source=(https://github.com/arvidn/${name%-*}/archive/$version/$name-$version.tar.gz)

build() {
	cd ${name%.*}-$version

	./configure           \
		--prefix=/usr \
		--enable-python-binding

	make V=1
	make DESTDIR=$PKG install
}
