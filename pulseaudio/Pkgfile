# Description:  Sound system for POSIX OSes
# URL:          http://www.freedesktop.org/wiki/Software/PulseAudio/
# Maintainer:   Alexandr Savca, drop at chinarulezzz dot fun
# Depends on:   dbus glib xorg-libsm xorg-libxtst libsndfile intltool consolekit sbc bluez

name=pulseaudio
version=13.99.2
release=1
source=(https://freedesktop.org/software/$name/releases/$name-$version.tar.xz) 

build() {
	cd $name-$version

	./configure --prefix=/usr \
		--libexecdir=/usr/lib/$name \
		--sysconfdir=/etc \
		--enable-bluez5 \
		--disable-bluez4 \
		--disable-default-build-tests \
		--disable-gtk3 \
		--disable-rpath \
		--disable-tcpwrap \
		--disable-hal-compat \
		--disable-openssl

	make V=1
	make -j1 DESTDIR=$PKG install

	rm -r $PKG/usr/share/locale
}
