# Description: Library for manipulating sound files
# URL:         http://www.mega-nerd.com/libsndfile
# Maintainer:  Stamatin Cristina, dear dot volgk at gmail dot com
# Depends on:  alsa-lib flac libvorbis autogen
# Optional:    sqlite3

name=libsndfile
version=1.0.30
release=1
source=(https://github.com/$name/$name/archive/v$version/$name-$version.tar.gz)

build() {
	cd $name-$version

	./autogen.sh
	./configure          \
		--prefix=/usr    \
		--disable-static \
		--with-pic

	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/share/doc
}
