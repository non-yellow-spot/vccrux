# Description: CRUX ports collections (via git transport)
# URL:         http://crux.nu
# Maintainer:  Alexandr Savca, alexandr dot savca89 at gmail dot com
# Depends on:  git

name=ports-via-git
version=3.5
release=1

source=(core.git
	opt.git
	xorg.git
	compat-32.git.inactive
	contrib.git.inactive)

build () {
	install -d $PKG/etc/ports
	install -m 0644 $SRC/core.git                $PKG/etc/ports
	install -m 0644 $SRC/opt.git                 $PKG/etc/ports
	install -m 0644 $SRC/xorg.git                $PKG/etc/ports
	install -m 0644 $SRC/compat-32.git.inactive  $PKG/etc/ports
	install -m 0644 $SRC/contrib.git.inactive    $PKG/etc/ports
}
