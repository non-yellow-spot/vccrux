# Description: Tools to assess WiFi network security
# URL:         https://github.com/aircrack-ng/aircrack-ng
# Maintainer:  Alexandr Savca, drop at chinarulezzz dot fun
# Depends on:  ethtool libnl libpcre sqlite3 hwloc libpcap

name=aircrack-ng
version=1.6
release=2
source=(https://github.com/$name/$name/archive/$version/$name-$version.tar.gz
	add-manufacturer-col-to-client-list.patch)

build() {
	cd $name-$version

	patch -p1 < $SRC/add-manufacturer-col-to-client-list.patch

	NOCONFIGURE=1 ./autogen.sh
	./configure \
		--prefix=/usr \
		--libexecdir=/usr/lib

	make V=1
	make DESTDIR=$PKG install
}
